<div id="{{this.id}}" class="profile-dropdown {{this.open}}">
	<div id="items">
		{{#each items}}
			<div class="profile-dropdown-item" id="{{this.id}}">
        {{this.name}}
      </div>
		{{/each}}
	</div>
</div>
